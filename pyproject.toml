[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]
per-file-ignores = {"app.py" = ["E402", "F401"]}

[tool.ruff.lint.isort]
known-first-party = ["codeup"]

[tool.pytest.ini_options]
testpaths = ["tests"]
timeout = 30
